/* theme.ts */
import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  breakpoints: {
    base: "320px",
    sm: "480px",
    md: "768px",
    lg: "1024px",
    xl: "1439px",
    "2xl": "1600px",
  },
});
